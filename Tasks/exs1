/*Костя подключен к мобильному оператору «Мобайл». Абонентская плата Кости составляет  рублей в месяц. 
За эту стоимость Костя получает  мегабайт интернет-трафика. Если Костя выйдет за лимит трафика,
то каждый следующий мегабайт будет стоить ему  рублей.
 Костя планирует потратить  мегабайт интернет-трафика в следующий месяц. 
Помогите ему сосчитать, во сколько рублей ему обойдется интернет.*/

if let line = readLine() { //считываем с клавиатуры строку и сразу ее безопасно раскрываем
    let lineArr = line.split(separator: " ") //разделяем строку по пробелу на 4 строки и записываем их в массив

    if let a = Int(lineArr[0]), //стоимость тарифа
       let b = Int(lineArr[1]), //размер тарифа
       let c = Int(lineArr[2]), //стоимость каждого лишнего мегабайта
       let d = Int(lineArr[3]) { //размер интернет трафика
//безопасно извлекаем элементы массива и сразу же преобразуем их из строк в целые числа

        // Считаем перерасход
        var totalCost = a //в любом случае он заплатит стоимость тарифа
        if d > b { //дальше условие использавал он интернета больше или нет
            totalCost += (d - b) * c //если больше, то к стоимости тарифа прибавляется сумма, которую он заплатит за лишние мегабайты
        }

        // Выводим итог
        print(totalCost)
    } else {
        print("Ошибка: неверный формат входных данных") //ошибка если не все значения введены правильно или если просто не введены все значения
    }
} else {
